# BSOD Analyzer 配置文件示例
# 复制此文件为 .env 并填入你的配置值

# ============================================================
# AI 分析配置
# ============================================================

# 智谱 AI API 密钥（必需）
# 获取地址: https://open.bigmodel.cn/
ZHIPU_API_KEY=your_api_key_here

# AI 模型名称（可选，默认: glm-4.7）
# 可用模型: glm-4.7, glm-4-plus, glm-4-air, glm-4-flash
AI_MODEL=glm-4.7

# AI 分析最大输出令牌数（可选，默认: 8192）
AI_MAX_TOKENS=8192

# ============================================================
# 数据库配置
# ============================================================

# SQLite 数据库文件路径（可选，默认: ~/.bsod_analyzer/crashes.db）
# Windows 示例: C:/Users/YourName/.bsod_analyzer/crashes.db
DATABASE_PATH=~/.bsod_analyzer/crashes.db

# ============================================================
# 日志配置
# ============================================================

# 日志级别（可选，默认: INFO）
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径（可选，默认: ~/.bsod_analyzer/bsod_analyzer.log）
# LOG_FILE=C:/Users/YourName/.bsod_analyzer/bsod_analyzer.log

# ============================================================
# 分析配置
# ============================================================

# 默认转储文件目录
DEFAULT_DUMP_DIR=C:/Windows/Minidump

# 批量分析时的最大文件数（可选，默认: 10）
BATCH_MAX_FILES=10

# 最大堆栈帧显示数量
MAX_STACK_FRAMES=10

# 是否默认启用 AI 分析（可选，默认: false）
DEFAULT_AI_ENABLED=false

# 置信度阈值（可选，默认: 0.5）
# 低于此值的结果将标记为"低置信度"
CONFIDENCE_THRESHOLD=0.5

# ============================================================
# 高级配置
# ============================================================

# 自定义驱动知识库路径（可选）
# DRIVER_KNOWLEDGE_PATH=./custom_drivers.json

# 是否启用调试模式（可选，默认: false）
# 调试模式会输出更详细的分析过程信息
DEBUG_MODE=false
